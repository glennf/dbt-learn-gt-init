sources:
  - name: stripe
    database: dbt-tutorial
    schema: stripe
    description: A clone of Stripe payments database
    tables:
      - name: payment
        description: Raw Stripe payments
        config:
          loaded_at_field: _batched_at
          freshness:
            warn_after: {count: 12, period: hour}
            error_after: {count: 24, period: hour}